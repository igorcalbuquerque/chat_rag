fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.1
redis==5.2.1
numpy==1.26.4
pypdf==5.1.0
python-docx==1.1.2
tiktoken==0.8.0

# Optional OAuth login (Google/GitHub). Active only when AUTH_ENABLED=true;
# harmless when off. Authlib uses the already-present httpx for token exchange.
authlib==1.3.2
itsdangerous==2.2.0

# LangChain / LangGraph orchestration
langchain==0.3.14
langchain-core==0.3.29
langchain-community==0.3.14
langchain-text-splitters==0.3.4
langchain-openai==0.2.14
langchain-anthropic==0.3.1
langchain-ollama==0.2.2
langchain-google-genai==2.0.8
langgraph==0.2.62

# Local open-source embeddings (sentence-transformers + torch) are OPTIONAL and
# heavy (~several GB). They are NOT installed in the default slim image. To use
# EMBEDDING_PROVIDER=sentence-transformers, install requirements-local.txt
# (or build with INSTALL_LOCAL_EMBEDDINGS=true).

# Tests
pytest==8.3.4
pytest-cov==6.0.0
pytest-asyncio==0.25.1
httpx==0.28.1
fakeredis==2.26.2
